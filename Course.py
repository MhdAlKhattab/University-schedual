# Stores data about course
class Course:
    # Initializes course
    def __init__(self, id, name, year, specialization):
        # Returns course ID
        self.Id = id
        # Returns course name
        self.Name = name
        # Returns course year
        self.Year = year
        # Returns course specialization
        self.Specialization = specialization
